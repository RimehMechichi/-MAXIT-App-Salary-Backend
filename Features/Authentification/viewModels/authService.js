/*const User = require('../models/UserModel.js');

// Create
async function signin(data) {
  return await User.create(data);
}

// Read All
async function signout() {
  return await User.find();
}

// Read One
async function forgotPassword(idUser) {
  return await User.findOne({ idUser });
}

// Update
async function resetPassword(idUser, data) {
  return await User.findOneAndUpdate({ idUser }, data, { new: true });
}


module.exports = {
  createUser,
  getAllUsers,
  getUserById,
  updateUser,
};
*/